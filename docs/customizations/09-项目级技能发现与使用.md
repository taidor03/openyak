# 09 — 项目级技能发现与使用

> 定制编号：XFLOW-017
> 关联模块：backend/app/api/skills.py, backend/app/skill/, frontend/src/hooks/use-plugins.ts, frontend/src/components/activity/activity-panel.tsx, frontend/src/components/parts/reasoning-part.tsx, frontend/src/components/workspace/context-section.tsx, frontend/src/app/(main)/plugins/content.tsx

---

## 1. 概述

上游 OpenYak 已实现技能注册表 (`SkillRegistry`)、`skill` 工具（LLM 按需加载技能）、系统提示注入 (`render_skills_section`)、以及前端技能列表/启用/禁用 UI。

**本次定制补全了以下缺失环节：**

1. 后端 `list_skills` API 增加 `workspace_path` 参数
2. 后端新增技能 CRUD 端点（create/update/delete）
3. 后端 `_skill_source` 增加 `agent` 来源识别
4. 后端 `get_skill` 详情 API 返回 `source`/`enabled` 字段
5. 前端 `useSkills` 支持 `workspacePath` 参数
6. 前端新增 `useCreateSkill`/`useDeleteSkill`/`useUpdateSkill`/`useSkillDetail` hooks
7. 前端新增 SKILLS.CREATE/DELETE/UPDATE/DETAIL API 常量
8. 活动面板增加 `skill` 工具图标和标题
9. 推理行增加 `skill` 工具标签
10. `SkillInfo.source` 类型增加 `"agent"` 值
11. `SkillsSummary` 组件增加来源分类展示
12. 插件页技能分组增加 `agent` 分类

---

## 2. 后端定制

### 2.1 `_skill_source()` 增加 `agent` 来源识别

**文件**：`backend/app/api/skills.py`

```python
def _skill_source(skill_name: str, location: str) -> str:
    """Determine the source of a skill: 'plugin', 'bundled', 'agent', or 'project'."""
    if ":" in skill_name:
        return "plugin"
    if "/data/skills/" in location or "\\data\\skills\\" in location:
        return "bundled"
    # Skills from .agents/<agent-name>/skills/ are per-agent skills
    if "/.agents/" in location or "\\.agents\\" in location:
        return "agent"
    return "project"
```

`.agents/<agent-name>/skills/` 目录下的技能被识别为 `agent` 来源，与 `.openyak/skills/` 的 `project` 来源区分。

### 2.2 `list_skills` 增加 `workspace_path` 查询参数

**文件**：`backend/app/api/skills.py`

```python
@router.get("/skills")
async def list_skills(
    registry: SkillRegistryDep,
    workspace_path: str | None = None,
) -> list[dict[str, Any]]:
    """List all discovered skills.

    If *workspace_path* is provided and differs from the registry's
    current project directory, the registry is rescanned for that
    project so that per-project / per-agent skills are up to date.
    """
    if workspace_path:
        resolved = Path(workspace_path).resolve()
        if resolved.is_dir():
            registry_dir = Path(registry._project_dir).resolve() if registry._project_dir else None
            if registry_dir != resolved:
                registry.scan(project_dir=str(resolved))
    return [_skill_to_dict(skill, registry) for skill in registry.all_skills()]
```

### 2.3 `get_skill` 详情 API 增加 `source`/`enabled` 字段

**文件**：`backend/app/api/skills.py`

```python
return {
    "name": skill.name,
    "description": skill.description,
    "location": skill.location,
    "source": _skill_source(skill.name, skill.location),
    "enabled": not registry.is_disabled(skill_name),
    "content": skill.content,
}
```

### 2.4 新增技能 CRUD 端点

**文件**：`backend/app/api/skills.py`

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/skills/create` | POST | 创建项目级/Agent 级技能文件 |
| `/api/skills/{skill_name}` | PUT | 更新技能内容（保留 frontmatter） |
| `/api/skills/{skill_name}` | DELETE | 删除技能文件及目录 |

#### CreateSkillRequest

```python
class CreateSkillRequest(BaseModel):
    """Body for POST /api/skills/create."""
    name: str
    description: str
    content: str = ""
    target: str = "project"  # 创建位置
    workspace_path: str | None = None
```

#### UpdateSkillRequest

```python
class UpdateSkillRequest(BaseModel):
    """Body for PUT /api/skills/{skill_name}."""
    content: str
```

#### 辅助函数

```python
def _resolve_project_dir(registry: SkillRegistry, workspace_path: str | None = None) -> Path | None:
    """Resolve project directory, preferring frontend-provided workspace_path."""
    if workspace_path:
        resolved = Path(workspace_path).resolve()
        if resolved.is_dir():
            return resolved
    if registry._project_dir:
        return Path(registry._project_dir)
    return None

def _skill_target_dir(target: str, project_dir: Path, slug: str) -> Path:
    """Resolve target directory for a new skill based on target type."""
    if target == "project":
        return project_dir / ".openyak" / "skills" / slug
    elif target == "agents":
        return project_dir / ".agents" / "skills" / slug
    else:
        # target is a specific agent name
        return project_dir / ".agents" / target / "skills" / slug
```

#### create_skill 关键逻辑

```python
async def create_skill(registry, body: CreateSkillRequest) -> dict:
    project_dir = _resolve_project_dir(registry, body.workspace_path)
    if not project_dir:
        raise HTTPException(400, "No project directory")
    slug = body.name.lower().replace(" ", "-")
    target_dir = _skill_target_dir(body.target, project_dir, slug)
    target_dir.mkdir(parents=True, exist_ok=True)
    skill_path = target_dir / "SKILL.md"

    if skill_path.exists():
        raise HTTPException(409, f"Skill '{name}' already exists at {skill_path}")

    # Build SKILL.md content with YAML frontmatter
    frontmatter = f"---\nname: {name}\ndescription: {body.description.strip()}\n---\n\n"
    skill_path.write_text(frontmatter + body.content, encoding="utf-8")

    # Re-scan so the new skill is picked up
    registry.scan(project_dir=str(project_dir))
```

#### update_skill 关键逻辑

```python
async def update_skill(registry, skill_name, body: UpdateSkillRequest) -> dict:
    skill = registry.get(skill_name)
    if skill is None:
        raise HTTPException(404)
    source = _skill_source(skill_name, skill.location)
    if source == "bundled":
        raise HTTPException(403, "Cannot edit bundled skills")

    existing = skill_path.read_text(encoding="utf-8")
    # Preserve frontmatter if present
    if existing.startswith("---"):
        end = existing.find("---", 3)
        if end != -1:
            frontmatter = existing[:end + 3]
            new_content = frontmatter + "\n\n" + body.content
    else:
        new_content = body.content

    skill_path.write_text(new_content, encoding="utf-8")
    registry.scan(project_dir=str(project_dir))
```

#### delete_skill 关键逻辑

```python
async def delete_skill(registry, skill_name) -> dict:
    skill = registry.get(skill_name)
    if skill is None:
        raise HTTPException(404)
    source = _skill_source(skill_name, skill.location)
    if source == "bundled":
        raise HTTPException(403, "Cannot delete bundled skills")

    skill_dir = Path(skill.location).parent
    # Remove the skill directory (SKILL.md + any bundled files)
    if skill_dir.is_dir():
        shutil.rmtree(skill_dir, ignore_errors=True)

    registry.unregister(skill_name)
```

---

## 3. 前端定制

### 3.1 API 常量扩展

**文件**：`frontend/src/lib/constants.ts`

```typescript
SKILLS: {
  LIST: "/api/skills",
  DETAIL: (name: string) => `/api/skills/${name}`,
  CREATE: "/api/skills/create",
  UPDATE: (name: string) => `/api/skills/${name}`,
  DELETE: (name: string) => `/api/skills/${name}`,
  ENABLE: (name: string) => `/api/skills/${name}/enable`,
  DISABLE: (name: string) => `/api/skills/${name}/disable`,
  STORE_SEARCH: "/api/skills/store/search",
  INSTALL: "/api/skills/install",
},
```

### 3.2 `SkillInfo` 类型扩展

**文件**：`frontend/src/types/plugins.ts`

```typescript
export interface SkillInfo {
  name: string;
  description: string;
  location: string;
  source: "bundled" | "plugin" | "agent" | "project";
  enabled: boolean;
}
```

### 3.3 `useSkills` 支持 `workspacePath` 参数

**文件**：`frontend/src/hooks/use-plugins.ts`

```typescript
export function useSkills(workspacePath?: string | null) {
  return useQuery({
    queryKey: [...queryKeys.skills, workspacePath ?? ""],
    queryFn: () => {
      const params = workspacePath
        ? `?workspace_path=${encodeURIComponent(workspacePath)}`
        : "";
      return api.get<SkillInfo[]>(`${API.SKILLS.LIST}${params}`);
    },
    staleTime: 30_000,
    refetchInterval: 60_000,
    meta: { persist: true },
  });
}
```

### 3.4 新增技能 CRUD Hooks

**文件**：`frontend/src/hooks/use-plugins.ts`

| Hook | 功能 | API 端点 |
|------|------|---------|
| `useCreateSkill()` | 创建技能 | POST `/api/skills/create` |
| `useUpdateSkill()` | 更新技能内容 | PUT `/api/skills/{name}` |
| `useDeleteSkill()` | 删除技能 | DELETE `/api/skills/{name}` |
| `useSkillDetail(name)` | 查询技能详情（含 content） | GET `/api/skills/{name}` |

**`CreateSkillPayload` 接口**：

```typescript
export interface CreateSkillPayload {
  name: string;
  description: string;
  content?: string;
  target?: "project" | "agents" | string;
  workspacePath?: string | null;
}
```

### 3.5 活动面板增加 `skill` 工具支持

**文件**：`frontend/src/components/activity/activity-panel.tsx`

```typescript
import { Sparkles } from "lucide-react";

const TOOL_ICONS = {
  // ...existing
  skill: Sparkles,  // 技能工具使用 Sparkles 图标
};

// getToolTitle 新增 case
case "skill":
  return input.name ? `Skill: ${input.name}` : "Load skill";
```

### 3.6 推理行增加 `skill` 工具标签

**文件**：`frontend/src/components/parts/reasoning-part.tsx`

```typescript
case "skill":
  return input.name ? `Skill: ${input.name}` : t("toolLoadSkill");
```

**i18n 翻译**：

- 中文：`"toolLoadSkill": "加载技能"`
- 英文：`"toolLoadSkill": "Load skill"`

### 3.7 `SkillsSummary` 组件增强

**文件**：`frontend/src/components/workspace/context-section.tsx`

- 显示 Sparkles 图标 + "Skills" 标签 + 总数
- 按来源分类展示：
  - 🟣 `project` + `agent` → "N projects"
  - 🔵 `bundled` → "N bundled"
  - 🟢 `plugin` → "N plugins"
- 传递 `workspacePath` 到 `useSkills`，确保项目切换后技能列表刷新

### 3.8 插件页技能分组增加 `agent` 分类

**文件**：`frontend/src/app/(main)/plugins/content.tsx`

- 新增 `agent` 技能过滤和 `filteredAgent` 分组
- `SOURCE_COLORS` 新增 `agent: "bg-violet-500/10 text-violet-400"`
- 技能列表中增加 "Agent Skills" 分组
- 传递 `workspacePath` 到 `useSkills`

---

## 4. 数据流

```
用户打开项目 → activeWorkspacePath 更新
    → useSkills(workspacePath) → GET /api/skills?workspace_path=...
    → 后端 list_skills 对比 registry._project_dir
    → 不同则 registry.scan(project_dir=新路径)
    → SkillRegistry 递归扫描:
        .openyak/skills/**/SKILL.md     → source=project
        .agents/<name>/skills/SKILL.md  → source=agent
        .claude/skills/SKILL.md         → source=project (外部兼容)
    → 返回技能列表 → LLM 系统提示注入 render_skills_section()
    → LLM 调用 skill 工具 → 活动面板显示 ✨ Skill: xxx
```

---

## 5. 配置常量表

| 常量 | 值 | 文件 |
|------|-----|------|
| `_skill_target_dir` project | `{project_dir}/.openyak/skills/{slug}/SKILL.md` | `backend/app/api/skills.py` |
| `_skill_target_dir` agents | `{project_dir}/.agents/skills/{slug}/SKILL.md` | `backend/app/api/skills.py` |
| `_skill_target_dir` agent | `{project_dir}/.agents/{target}/skills/{slug}/SKILL.md` | `backend/app/api/skills.py` |
| `SOURCE_COLORS.agent` | `"bg-violet-500/10 text-violet-400"` | `frontend/src/app/(main)/plugins/content.tsx` |

---

## 6. 测试要点

| 测试场景 | 验证方式 |
|---------|---------|
| 项目技能发现 | 在 `.openyak/skills/` 下放置 SKILL.md → 确认返回 source=project |
| Agent 技能发现 | 在 `.agents/build/skills/` 下放置 SKILL.md → 确认返回 source=agent |
| 创建技能 | POST `/api/skills/create` → 确认文件写入磁盘并返回 skill 对象 |
| 重复创建 | 同名再次创建 → 返回 409 |
| 更新技能 | PUT `/api/skills/{name}` → 确认 frontmatter 保留、内容更新 |
| 删除技能 | DELETE `/api/skills/{name}` → 确认目录移除、注册表注销 |
| 禁止编辑内置 | 对 bundled 技能调用 PUT/DELETE → 返回 403 |
| 活动面板展示 | LLM 调用 skill 工具 → 面板显示 ✨ 图标和 "Skill: xxx" |
| 项目切换刷新 | 切换 workspace → 技能列表自动重新扫描 |

---

## 7. 涉及文件

| 文件 | 变更类型 |
|------|---------|
| `backend/app/api/skills.py` | 修改（+workspace_path, +CRUD, +agent source） |
| `frontend/src/lib/constants.ts` | 修改（+DETAIL/CREATE/UPDATE/DELETE 常量） |
| `frontend/src/types/plugins.ts` | 修改（+agent source 类型） |
| `frontend/src/hooks/use-plugins.ts` | 修改（+workspacePath, +CRUD hooks） |
| `frontend/src/components/activity/activity-panel.tsx` | 修改（+skill 图标/标题） |
| `frontend/src/components/parts/reasoning-part.tsx` | 修改（+skill 标签） |
| `frontend/src/components/workspace/context-section.tsx` | 修改（+来源分类展示） |
| `frontend/src/app/(main)/plugins/content.tsx` | 修改（+agent 分组, +workspacePath） |
| `frontend/src/i18n/locales/zh/chat.json` | 修改（+toolLoadSkill） |
| `frontend/src/i18n/locales/en/chat.json` | 修改（+toolLoadSkill） |
