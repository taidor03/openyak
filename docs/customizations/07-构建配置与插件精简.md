# 07 - 构建配置与插件精简

> **涉及提交**: `7e33620`, `54f4420`, `a7d6ac4`  
> **涉及范围**: Tauri 构建配置 + 连接器/插件精简 + .gitignore

---

## 一、功能概述

本模块记录了项目构建部署配置的定制化调整，以及连接器/插件/技能目录的精简，确保桌面端只包含 Xflow 业务相关内容：

1. **Tauri 构建配置同步**：构建命令、更新器、资源路径、签名
2. **连接器精简**：移除 Xflow 无关的 OAuth 连接器
3. **插件精简**：删除无关业务插件，保留工程相关
4. **默认端口与配置调整**

> **提示**：Superpowers 插件移除已不作为定制项（上游已默认不内置），相关内容已从本文档移除。

---

## 二、Tauri 构建配置同步（7e33620）

### 2.1 构建命令

**`desktop-tauri/src-tauri/tauri.conf.json`**：

```json
{
  "build": {
    "beforeBuildCommand": "npm --prefix .. run build:frontend && npm --prefix .. run build:backend"
  }
}
```

- 构建前自动编译前端和后端
- 使用 `--prefix ..` 从项目根目录执行

### 2.2 更新器配置

```json
{
  "bundle": {
    "createUpdaterArtifacts": true  // 从 false 改为 true
  }
}
```

启用自动更新产物生成。

### 2.3 资源路径

```json
{
  "bundle": {
    "resources": {
      "../../backend/dist/openyak-backend": "backend",  // 相对路径
      "../../backend/resources/nodejs": "nodejs"         // 相对路径
    }
  }
}
```

从绝对路径改为相对路径，确保团队协作时不同开发者机器上都能正常构建。

### 2.4 签名配置

```json
{
  "macOS": {
    "signingIdentity": "Developer ID Application: wangzhang wu (46KF5Z549N)"
  }
}
```

从 `null` 改为具体的开发者签名身份。

---

## 三、连接器精简（54f4420）

### 3.1 保留的连接器

**`backend/app/data/connectors.json`** 仅保留以下连接器：

| 连接器 | 类别 | 说明 |
|--------|------|------|
| Slack | 通讯 | 团队协作 |
| Notion | 知识库 | 文档管理 |
| GitHub | 开发 | 代码仓库 |
| Datadog | 监控 | 可观测性 |
| Figma | 设计 | 设计协作 |
| Canva | 设计 | 创意工具 |
| BigQuery | 数据 | 数据分析 |

### 3.2 移除的连接器类别

- CRM 类（Salesforce、HubSpot 等）
- 分析类（Google Analytics、Mixpanel 等）
- 营销类（Mailchimp、HubSpot Marketing 等）
- 销售类（Outreach、Salesloft 等）
- 法务类（Ironclad、DocuSign 等）
- 运营类（PagerDuty、Opsgenie 等）
- 知识库类（Confluence 等）
- 生物研究类
- 客户支持类
- 企业搜索类

### 3.3 保留插件清理

保留的插件（engineering/productivity/design/data/product-management/finance/human-resources）的 `.mcp.json` 同步清理，移除不在 catalog 中的 MCP 引用，消除"其它"分类中的游离连接器。

---

## 四、插件删除（54f4420）

### 4.1 删除的插件目录

| 插件 | 说明 |
|------|------|
| bio-research | 生物研究 |
| legal | 法务 |
| marketing | 营销 |
| sales | 销售 |
| customer-support | 客户支持 |
| enterprise-search | 企业搜索 |
| operations | 运营 |

每个插件目录包含：`.claude-plugin/`、`.mcp.json`、`CONNECTORS.md`、`README.md`、`skills/` 等。

### 4.2 影响统计

163 个文件被删除，约 36,159 行代码移除。

---

## 五、其他配置调整

### 5.1 默认端口（5e82ad0）

**XFLOW-005**：默认端口从原值改为 8090

涉及文件：
- `backend/app/config.py`
- `backend/run.py`
- `frontend/src/lib/constants.ts`
- `package.json`

### 5.2 .gitignore 调整（a7d6ac4）

新增忽略规则，确保构建产物和敏感文件不被提交。

### 5.3 backend/.env.example（a7d6ac4）

新增 `.env.example` 文件（18 行），包含所有环境变量模板和注释说明。

### 5.4 i18n 默认语言（5e82ad0）

**XFLOW-006**：`frontend/src/i18n/config.ts` 设置默认语言为 `zh`。

---

## 六、前端 API 常量扩展

**`frontend/src/lib/constants.ts`** 中新增的 API 端点常量：

```typescript
API.MCP.USER_CONFIG = "/api/mcp/user-config";       // MCP 用户配置
API.LIVEZ = "/livez";                                 // 轻量无认证存活探测
API.STARTUP_STATUS = "/startup-status";               // 启动状态

API.SESSIONS.LIST = (limit, offset, includeArchived)  // 新增 includeArchived 参数

queryKeys.mcpConfig = ["mcpConfig"] as const;         // MCP 配置查询键
```

---

## 七、涉及文件清单

| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| `desktop-tauri/src-tauri/tauri.conf.json` | 修改 | 构建命令/更新器/资源路径/签名 |
| `backend/app/data/connectors.json` | 修改 | 精简至 Xflow 相关连接器 |
| `backend/app/data/plugins/{7个}/` | 删除 | 无关业务插件 |
| `backend/app/config.py` | 修改 | 默认端口 8090 |
| `backend/run.py` | 修改 | 默认端口 8090 |
| `frontend/src/lib/constants.ts` | 修改 | 默认端口 8090 + API 常量扩展 |
| `frontend/src/i18n/config.ts` | 修改 | 默认语言 zh |
| `package.json` | 修改 | 端口 |
| `.gitignore` | 修改 | 忽略规则 |
| `backend/.env.example` | 新增 | 环境变量模板 |
| 保留插件的 `.mcp.json` | 修改 | 清理游离连接器引用 |

---

## 八、重新实现检查清单

- [ ] `tauri.conf.json` 构建前命令（build:frontend + build:backend）
- [ ] `createUpdaterArtifacts: true`
- [ ] 资源路径改为相对路径（`../../backend/dist/...`）
- [ ] macOS 签名身份配置
- [ ] `connectors.json` 精简至 Xflow 相关（Slack/Notion/GitHub/Datadog/Figma/Canva/BigQuery）
- [ ] 删除无关插件（7 个业务无关插件目录）
- [ ] 保留插件的 `.mcp.json` 清理游离连接器
- [ ] 默认端口改为 8090（config.py / run.py / constants.ts / package.json）
- [ ] i18n 默认语言设为 zh
- [ ] `.gitignore` 忽略规则完善
- [ ] `backend/.env.example` 环境变量模板
- [ ] `API.MCP.USER_CONFIG` 常量
- [ ] `API.LIVEZ` / `API.STARTUP_STATUS` 常量
- [ ] `API.SESSIONS.LIST` 新增 `includeArchived` 参数
- [ ] `queryKeys.mcpConfig` 查询键
