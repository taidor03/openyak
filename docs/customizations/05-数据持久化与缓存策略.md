# 05 - 数据持久化与缓存策略

> **定制编号**: XFLOW-013（部分）+ e471750  
> **涉及范围**: TanStack Query 持久化 + 会话缓存 + 连接器/插件/技能即时缓存同步

---

## 一、功能概述

本模块记录了桌面端冷启动体验优化的核心数据持久化策略，目标是"冷启动即显即刷"：

1. **TanStack Query 持久化**：通用查询缓存持久化到 localStorage
2. **会话列表本地缓存**：侧边栏即时渲染
3. **连接器/插件/技能即时缓存同步**：mutation 操作即时更新本地缓存，消除 invalidateQueries 闪烁

> **提示**：Provider/Model 本地缓存已独立至 [08-Provider与模型管理.md](./08-Provider与模型管理.md)。

---

## 二、TanStack Query 持久化

### 2.1 依赖

```json
{
  "@tanstack/query-sync-storage-persister": "^5.x",
  "@tanstack/react-query-persist-client": "^5.x"
}
```

### 2.2 持久化模块

**新增文件**：`frontend/src/lib/query-persister.ts`

选择性持久化层，仅持久化 `meta.persist=true` 的查询：

```typescript
/**
 * PERSISTED queries (shown from cache on cold start):
 *   - agents list, connectors list, plugins list, skills list, mcp config
 *
 * NOT persisted (always fetched fresh):
 *   - chat messages, usage stats, SSE state, sessions (has its own cache)
 *
 * A query opts-in by setting `meta: { persist: true }` in useQuery options.
 */

import { dehydrate, type DehydratedState } from "@tanstack/react-query";
import { persistQueryClient } from "@tanstack/react-query-persist-client";
import { createSyncStoragePersister } from "@tanstack/query-sync-storage-persister";

export const PERSIST_MAX_AGE_MS = 24 * 60 * 60 * 1000; // 24h

const STORAGE_KEY = "openyak_query_cache";

export const localStoragePersister = (() => {
  try {
    if (typeof window === "undefined" || !window.localStorage) return null;
    return createSyncStoragePersister({
      storage: window.localStorage,
      key: STORAGE_KEY,
      throttleTime: 1_000,  // Throttle writes so rapid invalidations don't flood storage
    });
  } catch {
    return null;
  }
})();

export function shouldPersistQuery(query: { meta?: Record<string, unknown> }): boolean {
  return query.meta?.persist === true;
}

export const dehydrateOptions: {
  shouldDehydrateQuery: (q: DehydratedState["queries"][number]) => boolean;
} = {
  shouldDehydrateQuery: (query) =>
    shouldPersistQuery(query) && query.state.status === "success",
};
```

### 2.3 设置 persist 的查询列表

以下查询设置了 `meta: { persist: true }`：

| Hook 文件 | 查询 |
|-----------|------|
| `use-agents.ts` | agents 列表 |
| `use-connectors.ts` | connectors 列表 |
| `use-plugins.ts` | plugins 列表 |
| `use-plugins.ts` | skills 列表 |
| `use-mcp-config.ts` | MCP 用户配置 |

### 2.4 Provider 集成

**`frontend/src/components/providers/query-provider.tsx`**：

```typescript
const [isRestoring] = useState(() => {
  setupQueryPersister(queryClient);
  return true;
});

// localStorage 可用时（非 SSR / 非隐私模式）
<PersistQueryClientProvider
  client={queryClient}
  persistOptions={{
    persister,
    maxAge: PERSIST_MAX_AGE_MS,
    dehydrateOptions,
  }}
>
```

---

## 三、会话列表本地缓存（XFLOW-013）

详见 [02-会话管理与UI增强.md](./02-会话管理与UI增强.md) 第四节。

使用独立的 `session-cache.ts`（key: `xflow:sessions_v1`），不通过 TanStack Query 持久化。

---

## 四、连接器/插件/技能即时缓存同步

### 4.1 问题

默认 `invalidateQueries` 会导致 UI 闪烁（旧数据消失 → loading → 新数据），因为后端响应有延迟。

### 4.2 解决方案

mutation 操作成功后，先用 `setQueryData` 即时更新缓存（乐观更新），再 `invalidateQueries` 后台静默刷新。

### 4.3 连接器即时同步

**`frontend/src/hooks/use-connectors.ts`**：

```typescript
onSuccess: (data) => {
  // 即时更新缓存
  queryClient.setQueryData(queryKeys.connectors, data);
  // 后台静默刷新
  queryClient.invalidateQueries({ queryKey: queryKeys.connectors });
},
```

### 4.4 MCP 配置即时同步

**`frontend/src/hooks/use-mcp-config.ts`**：

```typescript
onSuccess: (data) => {
  queryClient.setQueryData(queryKeys.mcpConfig, data);
  queryClient.invalidateQueries({ queryKey: queryKeys.mcpConfig });
},
```

### 4.5 插件/技能即时同步

**`frontend/src/hooks/use-plugins.ts`**：

```typescript
onSuccess: (data) => {
  queryClient.setQueryData(queryKeys.plugins, data);
  queryClient.invalidateQueries({ queryKey: queryKeys.plugins });
},
```

---

## 五、缓存架构总览

```
┌─────────────────────────────────────────────────┐
│              localStorage                       │
├─────────────────────────────────────────────────┤
│ openyak_query_cache                             │
│  ├ agents / connectors / mcpConfig              │
│  ├ plugins / skills                             │
│  └ (only meta.persist=true, success status)     │
├─────────────────────────────────────────────────┤
│ xflow:sessions_v1                               │
│  └ sessions flat list (独立缓存层)              │
├─────────────────────────────────────────────────┤
│ xflow:providers_v1 / xflow:models_v1            │
│  └ (Provider/Model 缓存，见08文档)              │
└─────────────────────────────────────────────────┘

写入流程: setQueryData(即时) → invalidateQueries(后台刷新)
读取流程: localStorage 缓存 → 即显 → 后台刷新 → UI 静默更新

注：Provider/Model 缓存已独立至 08-Provider与模型管理.md
```

---

## 六、配置常量表

| 常量 | 值 | 文件 |
|------|-----|------|
| `STORAGE_KEY` | `"openyak_query_cache"` | `frontend/src/lib/query-persister.ts` |
| `PERSIST_MAX_AGE_MS` | `24 * 60 * 60 * 1000`（24h） | `frontend/src/lib/query-persister.ts` |
| `throttleTime` | `1_000` ms | `frontend/src/lib/query-persister.ts` |
| `SESSIONS_KEY` | `"xflow:sessions_v1"` | `frontend/src/lib/session-cache.ts` |

---

## 七、涉及文件清单

| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| `frontend/src/lib/query-persister.ts` | 新增 | TanStack Query 持久化层 |
| `frontend/src/components/providers/query-provider.tsx` | 修改 | PersistQueryClientProvider 集成 |
| `frontend/src/lib/session-cache.ts` | 新增 | 会话缓存读写 |
| `frontend/src/hooks/use-connectors.ts` | 修改 | 即时缓存同步 + meta.persist |
| `frontend/src/hooks/use-mcp-config.ts` | 修改 | 即时缓存同步 + meta.persist |
| `frontend/src/hooks/use-plugins.ts` | 修改 | 即时缓存同步 + meta.persist |
| `frontend/src/hooks/use-agents.ts` | 修改 | meta.persist |

---

## 八、重新实现检查清单

- [ ] 安装 `@tanstack/query-sync-storage-persister` + `@tanstack/react-query-persist-client`
- [ ] `query-persister.ts`（STORAGE_KEY=`openyak_query_cache`, PERSIST_MAX_AGE_MS=24h, shouldPersistQuery 过滤）
- [ ] `query-provider.tsx` 集成 `PersistQueryClientProvider`
- [ ] 所有慢查询设置 `meta: { persist: true }`（agents/connectors/mcpConfig/plugins/skills）
- [ ] `session-cache.ts` 独立缓存（`xflow:sessions_v1`）
- [ ] mutation 成功后 `setQueryData` + `invalidateQueries` 双重更新
- [ ] 所有即时更新后追加 `invalidateQueries` 后台刷新

> **Provider/Model 缓存**检查清单已移至 [08-Provider与模型管理.md](./08-Provider与模型管理.md)。
